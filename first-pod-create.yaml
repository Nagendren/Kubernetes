apiVersion: v1
kind: Pod
metadata:
  name: myfirstpod
  labels:
    name: myapp
spec:
  containers:
  - name: myfirstpod-container
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
